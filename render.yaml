services:
  - type: web
    name: ytdlp-clipper
    runtime: python
    buildCommand: pip install -r requirements.txt && apt-get install -y ffmpeg
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
